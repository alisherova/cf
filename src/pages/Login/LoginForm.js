import LoginForm from "../../components/login";
import { Wrapper } from "./LoginForm.styled";
const Login = () => {
  return (
    <Wrapper>
      <LoginForm />
    </Wrapper>
  );
};

export default Login;
